# Third-party notices

The active `Assets/CommunityCore/` demo uses Unity engine APIs and the Unity packages declared in `Packages/manifest.json`. Unity package and editor terms apply separately; this repository does not relicense Unity.

The original archive contained additional packages, models, shaders, fonts, and other material. This mirror leaves those folders out of the active project because the archive does not provide a complete per-asset rights inventory. Do not add a model, texture, font, sound, editor extension, or package unless its source and license are recorded here.
